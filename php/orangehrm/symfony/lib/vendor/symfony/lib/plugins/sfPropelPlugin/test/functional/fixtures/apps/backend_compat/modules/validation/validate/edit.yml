methods:
  post:
    - "article{title}"
    - "article{body}"

names:
  article{title}:
    required:     yes
    required_msg: required

  article{body}:
    required:     yes
    required_msg: required
