save "wrong" as "username"
save "wrong" as "password"
login
check that page contains "Invalid credentials"
check that page doesn't contain "Dashboard"
check that page contains field "Username"
check that page contains field "Password"
